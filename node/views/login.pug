extends layout.pug

block content
    div(class="auth")
        h2 Sign in
        form(method="POST" action="/login")
              input(type="text" name="userId" value="admin")
              input(type="password" name="userId" value="admin")
              input(type="hidden" name="redirect" value=redirectUrl)
              p You can just hit Login in :)
              button(type="submit" class="primary") Login
